# Target Intelligence Context

**Статус:** На ревью
**Владелец:** Product Owner
**Дата последнего изменения:** 2026-08-12
**Назначение:** определить концептуальный «Analysis Context Package» — что именно собирается и проверяется до синтеза AI-инсайта, — чтобы будущие источники данных не добавлялись бессистемно напрямую в prompt.

---

## 0. Зачем отдельный документ, а не расширение существующего

Это отдельный вопрос от `INFORMATION_ARCHITECTURE.md` (тот документ — о навигации разделов продукта; этот — о том, какие данные вправе достичь LLM и на каких условиях) и отдельный вопрос от `docs/product/FUNCTIONAL_REQUIREMENTS.md` (тот фиксирует обязательные требования; этот — расширяемый инвентарь кандидатов, большинство из которых **не** являются требованиями сегодня). По `DOCUMENTATION_STANDARD.md`, раздел 8 (один источник истины на вопрос) и `PROJECT_CHARTER.md`, раздел 7.1 (одна зона ответственности на документ), смешивание этих трёх вопросов в одном файле создало бы неоднозначность. Настоящий документ детализирует уже утверждённые `LOGICAL_ARCHITECTURE.md` (области Source Integration → Ingestion → Normalization → Data Quality → Provenance → Analysis Orchestration → LLM Gateway) и `ENGINEERING_PRINCIPLES.md`, пункты 54–55 (запрет отправки нефильтрованного сырого потока в LLM) — он не создаёт новых логических областей.

**Ключевой принцип (Product Owner, Phase 0): AI-синтез — один компонент системы, а не вся система.** Ценность продукта — в цепочке «источник → нормализация → качество → отбор релевантности → контекст → provenance → синтез → сохранение → оценка результата» целиком (`MODULE_BOUNDARIES.md`, `LOGICAL_ARCHITECTURE.md`, раздел 7.1). Продукт не документируется и не проектируется как «отправить рыночные данные и новости в LLM».

**Настоящий документ не разрешает реализацию ни одной из перечисленных ниже категорий.** Он существует, чтобы последующие задачи классифицировали новую категорию данных explicitly, а не добавляли её в prompt по факту появления идеи.

---

## 1. Aналитический контекстный пакет — обзор

Формируется (концептуально, независимо от того, что физически реализовано сегодня) применительно к: **рынку** (`market`), **инструменту** (`identity`), **горизонту** (`horizon`) и **конкретной аналитической задаче** (ручной запрос анализа, FR-016). Ни одна категория ниже не считается автоматически полезной только по факту существования — раздел 3 явно требует классификации «больше данных ≠ лучше анализ».

---

## 2. Категории контекста

### 2.1 identity — идентичность инструмента

- **Текущая доступность:** доступно (`ticker`, `name`, `exchange`, `currency`, `instrument_type` — через `instruments/search`, `watchlist`) для US-listed акций.
- **Стадия:** **CURRENT** (US equities) / **MVP FOUNDATION** (глобальная идентичность инструмента — Phase 2.0, PO-2.0-1).
- **Источник:** `market_data` (сегодня), в перспективе `instruments`-каталог (`MODULE_BOUNDARIES.md`, раздел 3).
- **Фильтрация/релевантность:** не требуется.
- **Может входить в LLM-контекст:** да, уже входит (`ticker`).
- **При недоступности:** уже обрабатывается — нераспознанный тикер отклоняется до анализа (`422`, существующее поведение).
- **Global equities note (Phase 2.0, PO-2.0-1):** европейские и азиатские акции — **утверждённое направление расширения**, не просто гипотетическая идея Future, но реализация не начата и не разрешена этим документом. До реализации нерешены: кросс-листинг одной компании на нескольких биржах (какая биржа — источник истины для идентичности), нормализация валюты котировки (US equities сегодня всегда USD; EU/Asian — многовалютны), различающиеся биржевые календари/сессии (раздел 2.2). US equities остаются единственным полностью реализованным рынком акций.

### 2.2 market — принадлежность рынку и его семантика

- **Текущая доступность:** отсутствует как явное поле — сегодня подразумевается ровно один рынок (US-listed акции).
- **Стадия:** **MVP FOUNDATION** (нужно до/вместе с многорыночным расширением, `PO-2`); включает семантику часов торгов и 24×7 для крипты.
- **Источник:** зависит от `PO-2`/будущего `ADR-0011`.
- **Фильтрация/релевантность:** не применимо к самой категории; но конкретный рынок определяет, какие остальные категории вообще применимы (например, «часы торгов» неприменимы к крипте).
- **Может входить в LLM-контекст:** **не решено** — нужно ли явно называть модели текущий рыночный режим (открыт/закрыт рынок) — открытый вопрос для `ADR-0011`/будущей задачи горизонта.
- **При недоступности:** не применимо сегодня (один рынок).
- **CURRENT vs. APPROVED EXPANSION DIRECTION (Phase 2.0, PO-2.0-1, дословное разграничение Product Owner):**
  - **CURRENT:** US equities (US-listed акции) — единственный полностью работающий рынок акций сегодня.
  - **APPROVED EXPANSION DIRECTION (не implementation-разрешение):** европейские и азиатские акции. Это не означает обязательную поддержку каждой мировой биржи сразу — конкретный набор бирж/стран определяется отдельным продуктовым решением при постановке задачи реализации, после исследования провайдеров, дизайна идентичности инструмента, нормализации валюты, семантики торговых сессий/календарей и оценки лицензирования (все пять — прямое требование Product Owner, ни одно не выполнено этим документом).
  - Индексы, ставки/доходности и макропоказатели **не являются** этой категорией (`market`) — это отдельная категория **глобального рыночного контекста** (раздел 2.6, 2.16, 2.17), изначально не торгуемая пользователем «Markets»-секция, а объясняющий контекст (Phase 2.0, PO-2.0-2). Это не исключает будущих торгуемых производных (индексные фьючерсы/ETF) — это отдельный, не рассматриваемый здесь вопрос scope.

### 2.3 horizon — горизонт анализа

- **Текущая доступность:** отсутствует полностью в коде на момент этой задачи (не реализовано).
- **Стадия:** **MVP REQUIRED** (`FR-006`). **Phase 2.0 (PO-2.0-3) утвердил конкретные диапазоны** — SHORT = 1–5 торговых дней, MEDIUM ≈ 1–8 недель, LONG ≈ 2–12 месяцев (нормативно зафиксировано в `FUNCTIONAL_REQUIREMENTS.md`, FR-006). Это утверждение продуктовой семантики, **не завершение реализации** — горизонт по-прежнему не реализован в коде.
- **Источник:** выбор пользователя (Analysis Session Context, `LOGICAL_ARCHITECTURE.md`, раздел 13.1 — уже архитектурно предусмотрено).
- **Фильтрация/релевантность:** горизонт определяет, какой глубины price/history и каких ещё категорий этого списка достаточно для честного вывода — не наоборот. Концептуальные требования к окну данных на горизонт детализированы в `docs/architecture/FORECAST_CONTRACT.md`, раздел 7 — **точные минимальные размеры выборки намеренно не зафиксированы ни там, ни здесь**, остаются предметом суждения Solution Architect.
- **Может входить в LLM-контекст:** да, как только реализовано — обязательное требование `FR-006`: «горизонт передаётся в аналитический запрос и **влияет на структуру инсайта**», не только на текст интерфейса.
- **При недоступности достаточных данных для выбранного горизонта:** **обязательное поведение, а не опция** — уровень уверенности должен быть явно понижен, либо формируется явное состояние недостаточности (см. `FR-019`, а также будущее `NO_QUALITY_SETUP`/`INSUFFICIENT_DATA`, `FORECAST_CONTRACT.md`, раздел 5). Система не должна формировать уверенный LONG-анализ на основании одного месяца истории и нескольких заголовков новостей — это прямая, дословная формулировка Product Owner, подтверждённая повторно в Phase 2.0.
- **Явное разграничение:** SHORT-горизонт **не становится** скальпинг-режимом. Скальпинг (`FR-027`) остаётся отдельным, самостоятельно документированным быстрым режимом со своими правилами (пропуск уточняющих вопросов, упрощённый вывод) — а не синонимом «горизонт = короткий». Подтверждено повторно Phase 2.0 (PO-2.0-3).
- **Рыночно-специфичная семантика (Phase 2.0):** «торговый день»/«неделя»/«месяц» не эквивалентны на разных рынках — сессионные рынки (акции, сырьё) и непрерывный крипто-рынок (24/7) требуют разного понимания единицы горизонта; это не решается настоящим документом, только называется как требование к будущей реализации.

### 2.4 price / history — котировка и ценовая история

- **Текущая доступность:** доступно — котировка (OHLC/volume/as_of) и сводка истории за фиксированное окно 1 месяц (первая/последняя/мин/макс цена закрытия, без сырых точек).
- **Стадия:** **CURRENT** (для текущего недифференцированного поведения) / **MVP FOUNDATION** (для горизонт-зависимого окна — фиксированное окно в 1 месяц не масштабируется под LONG-горизонт; см. 2.3).
- **Источник:** `market_data` (Twelve Data).
- **Фильтрация/релевантность:** не требуется сверх существующей суммаризации.
- **Может входить в LLM-контекст:** да, уже входит.
- **При недоступности:** уже обрабатывается (`FR-059`, graceful degradation).

### 2.5 news — новости по инструменту

- **Текущая доступность:** доступно, до 10 полученных / до 5 переданных в модель, необработанный язык источника, без дедупликации, без ранжирования по релевантности.
- **Стадия:** **CURRENT** (сырой список) / **MVP FOUNDATION** (дедупликация `FR-015`, перевод `FR-060`, сигнал релевантности — ни одно не реализовано).
- **Источник:** `market_data`/`news_gateway` (Finnhub).
- **Фильтрация/релевантность:** **требуется до LLM** (`ENGINEERING_PRINCIPLES.md`, пункты 54–55) — сегодня отсутствует сверх собственной привязки провайдера к тикеру.
- **Может входить в LLM-контекст:** да, уже входит.
- **При недоступности:** уже обрабатывается (новости — опциональный источник в анализе).

### 2.6 macro — макроэкономический контекст

- **Текущая доступность:** отсутствует полностью.
- **Стадия:** **MVP FOUNDATION** (Phase 2.0, PO-2.0-2 — направление и категория одобрены Product Owner как «глобальный рыночный контекст»; конкретный источник данных по-прежнему **UNRESOLVED**). Изменение относительно Phase 0: тогда была не решена сама категория; теперь категория одобрена, не решён только источник.
- **Роль (Phase 2.0):** объясняющий контекст, а не торгуемая пользователем «Markets»-секция (PO-2.0-2, пример: «macro → economic context/events»).
- **Источник:** не выбран — требует исследования Solution Architect (см. `docs/architecture/TECHNOLOGY_EVALUATION.md`, раздел, добавленный этой задачей).
- **Фильтрация/релевантность:** потребуется обязательно при появлении (риск стоимости/шума, `PRODUCT_SCOPE.md`, раздел 29; `ENGINEERING_PRINCIPLES.md`, пункты 54–55). Макроданные регулярно пересматриваются («revised») после первой публикации — при появлении обязательна честная фиксация, какое именно значение (первичное/пересмотренное) использовано, как отдельный provenance-факт.
- **Может входить в LLM-контекст:** только как суммированная величина/изменение — никогда как сырой дамп релиза (раздел 3).
- **При недоступности:** не применимо сегодня; при появлении обязана деградировать честно (`FR-059`-подобно), не маскироваться.

### 2.7 calendar / events — экономический календарь и события

- **Текущая доступность:** отсутствует.
- **Стадия:** **Post-MVP** (`FR-010`, уже зафиксировано, не переоткрывается этой задачей).
- **Источник:** не выбран.
- **Фильтрация/релевантность:** потребуется.
- **Может входить в LLM-контекст:** нет, до Post-MVP реализации.
- **При недоступности:** раздел UI отображает «запланировано на более поздний этап» (см. `INFORMATION_ARCHITECTURE.md`, раздел 2.5).

### 2.8 earnings / company events — корпоративные события

- **Текущая доступность:** отсутствует.
- **Стадия:** **UNRESOLVED / REQUIRES PO DECISION** — ни один FR не называет эту категорию отдельно; не путать с общим календарём (`FR-010`).
- **Источник:** не выбран.
- **Может входить в LLM-контекст:** нет, требует сначала нового FR.

### 2.9 fundamentals — фундаментальные показатели

- **Текущая доступность:** отсутствует.
- **Стадия:** **UNRESOLVED / REQUIRES PO DECISION** — не задокументировано ни в одном FR.
- **Источник:** не выбран.
- **Может входить в LLM-контекст:** нет, требует сначала нового FR — не добавляется по умолчанию только потому, что технически доступно у провайдера.

### 2.10 derived / technical features — производные технические признаки

- **Текущая доступность:** частично — уже вычисляемые min/max/first/last цены закрытия существуют, но намеренно **не** подаются как «технический индикатор».
- **Стадия:** **UNRESOLVED / REQUIRES PO DECISION** для любого признака сверх уже существующей сводки (RSI, скользящие средние и подобное) — только «где обосновано» (прямая формулировка Product Owner), не по умолчанию.
- **Может входить в LLM-контекст:** уже входит в минимальной форме (min/max/first/last); дальнейшее расширение требует явного обоснования конкретной задачей.

### 2.11 sentiment / opinion — настроения/мнения

- **Текущая доступность:** отсутствует; упомянуто только неоперационально («мнения», `PRODUCT_SCOPE.md`, раздел 2).
- **Стадия:** **UNRESOLVED / REQUIRES PO DECISION.**
- **Условие Product Owner, если когда-либо реализуется:** источник и методика формирования оценки должны быть явными — категориальное значение с прослеживаемым происхождением, никогда «голое» число без объяснения (тот же принцип, что уже применён к `confidence`, `ai/types.py`).
- **Может входить в LLM-контекст:** нет, до отдельного решения.

### 2.12 sector / industry context — отраслевой контекст

- **Текущая доступность:** отсутствует; концепция «сектора» не существует нигде в текущей модели данных.
- **Стадия:** **UNRESOLVED / REQUIRES PO DECISION**, вероятно Post-MVP при появлении.
- **Может входить в LLM-контекст:** нет.

### 2.13 source quality / freshness — качество и актуальность источника

- **Текущая доступность:** частично — актуальность (`data_freshness`, `FR-012`) реализована и подаётся в модель backend-вычисленной строкой (никогда не запрашивается у самой модели); базовая оценка надёжности источника (`FR-013`) — COULD/Post-MVP, не реализована.
- **Стадия:** **CURRENT** (актуальность) / **Post-MVP** (надёжность источника).
- **Может входить в LLM-контекст:** актуальность — уже входит; надёжность — нет, до Post-MVP.

### 2.14 provenance — происхождение фактов

- **Текущая доступность:** частично — минимальная форма (`KeyFact.source`, метка-строка) реализована и обязательна (`FR-011`); полный набор полей происхождения вызова LLM по `ADR-0007`, раздел 46 (usage, finish_reason, retries, версия снапшота модели) **не персистируется** сегодня — задокументированный, честный пробел (см. checkpoint, часть 7).
- **Стадия:** **CURRENT** (минимальная форма) / **MVP FOUNDATION** (полнота по ADR-0007 §46 — небольшое, аддитивное расширение, не новая архитектура).
- **Может входить в LLM-контекст:** не применимо — provenance описывает вывод модели, не подаётся в модель.

### 2.15 prior insight / outcome evidence — данные прошлых инсайтов и результатов

- **Текущая доступность:** данные существуют и связаны (FK `insight_id` в `insight_evaluations`/`journal_entries`), но **никогда не читаются обратно** ни в один будущий вызов LLM.
- **Стадия:** **FUTURE**, жёстко закрыто условиями `ADR-0005`, раздел 19 (18 условий активации, ни одно не выполнено) — не открывается этой задачей.
- **Может входить в LLM-контекст:** нет, до отдельного явного решения об активации retrieval по `ADR-0005`.
- **При недоступности:** не применимо — концепция ещё не активирована.

### 2.16 indices — фондовые индексы (NEW, Phase 2.0)

- **Текущая доступность:** отсутствует полностью.
- **Стадия:** **MVP FOUNDATION** (направление одобрено, PO-2.0-2) — источник **UNRESOLVED**.
- **Роль:** объясняющий broad-market/режимный контекст (тренд, широта рынка), **не** торгуемый пользователем инструмент в этой роли (см. раздел 2.2). Отличие индекса как значения от торгуемого прокси (фьючерс/ETF на индекс) должно быть явным при реализации — эти два понятия не взаимозаменяемы.
- **Источник:** не выбран.
- **Фильтрация/релевантность:** требуется — индекс релевантен не каждому инструменту одинаково (например, отраслевой индекс релевантнее конкретной акции, чем широкий рыночный).
- **Может входить в LLM-контекст:** только как суммированное значение/изменение, никогда как сырой временной ряд (раздел 3).
- **При недоступности:** не применимо сегодня.

### 2.17 rates / yields — процентные ставки и доходности облигаций (NEW, Phase 2.0)

- **Текущая доступность:** отсутствует полностью.
- **Стадия:** **MVP FOUNDATION** (направление одобрено, PO-2.0-2, пример «rates/yields → cross-asset explanatory context») — источник **UNRESOLVED**.
- **Роль:** объясняющий кросс-активный контекст (например, для гипотез вида «ставки → USD → акции/золото», раздел 2.18), не отдельная торгуемая «Markets»-секция.
- **Источник:** не выбран, вероятно пересекается с источником раздела 2.6 (макро/календарь) — это гипотеза для будущего исследования провайдеров, не решение.
- **Фильтрация/релевантность:** требуется — конкретный тенор (2Y/10Y/etc.) релевантен по-разному в зависимости от заявленного горизонта анализа (раздел 2.3).
- **Может входить в LLM-контекст:** только как суммированный уровень/изменение, никогда как сырая кривая доходности (раздел 3).
- **При недоступности:** не применимо сегодня.

### 2.18 cross-asset relationship claims — кросс-рыночные заявления о связи (NEW, Phase 2.0)

- **Текущая доступность:** отсутствует.
- **Стадия:** **MVP FOUNDATION** для самой таксономии типов заявления (одобрена, PO-2.0-9); **UNRESOLVED** для любого конкретного зашитого правила причинности.
- **Роль:** не отдельная категория сырых данных, а обязательный способ маркировки любого заявления, связывающего разные рынки/активы (например, «Asia → Europe → US», «нефть → инфляционные ожидания → ставки → акции», «BTC ↔ широкий риск-аппетит»). Каждое такое заявление обязано нести один из пяти типов: `OBSERVED_RELATIONSHIP` / `CORRELATION` / `HYPOTHESIS` / `SUPPORTED_CAUSAL_INTERPRETATION` / `UNCERTAINTY` (подробно — `docs/architecture/FORECAST_CONTRACT.md`, раздел 6).
- **Источник:** производное от уже собранных категорий этого документа (2.2, 2.6, 2.16, 2.17 и др.), не отдельный внешний источник данных.
- **Фильтрация/релевантность:** обязательна — конкретные примеры отношений (перечисленные выше) являются иллюстративными кандидатами-шаблонами, не одобренными жёстко закодированными правилами; ни одно не поднимается до `SUPPORTED_CAUSAL_INTERPRETATION` по умолчанию.
- **Может входить в LLM-контекст:** нет как отдельная категория входных данных — это правило маркировки **выходных** заявлений (Forecast Contract), симметричное входному правилу раздела 3.
- **При недоступности:** не применимо — концепция не активирована до появления Forecast Contract (реализация отдельной задачей).

---

## 3. Явное правило: больше данных ≠ лучше анализ

Ни одна категория из раздела 2 не добавляется к тому, что уже отправляется в LLM, без явного прохождения через:

1. нормализацию (`normalization`, `LOGICAL_ARCHITECTURE.md`, раздел 7);
2. проверку качества/актуальности (`data_quality`);
3. отбор релевантности (совместно `data_quality`/`analysis`, `LOGICAL_ARCHITECTURE.md`, раздел 24);
4. привязку provenance (`provenance`).

Категории, помеченные **UNRESOLVED / REQUIRES PO DECISION**, не реализуются впрямую по мере появления идеи или доступности у провайдера — каждая требует отдельного продуктового решения (новый или расширенный FR) до того, как этап реализации станет осмысленным.

**Явное правило Phase 2.0 (raw vs. summarized):** ни одна из новых категорий этой задачи (2.16 indices, 2.17 rates/yields, 2.6 macro, 2.18 cross-asset claims) не входит в LLM-контекст как сырой провайдерский payload — только как уже нормализованная, отфильтрованная, суммированная величина или производный признак, той же дисциплиной, что уже применяется к price/history (раздел 2.4) и news (раздел 2.5). Одобрение категории (Phase 2.0) не означает одобрение передачи модели необработанного потока этой категории — это два разных решения.

---

## 4. Связанные документы

- `docs/architecture/LOGICAL_ARCHITECTURE.md`, раздел 7 (Source Integration → ... → LLM Gateway), раздел 24 (стоимость/предварительная фильтрация).
- `docs/architecture/MODULE_BOUNDARIES.md`, разделы 4–10 (`sources`…`llm_gateway`).
- `docs/architecture/ENGINEERING_PRINCIPLES.md`, пункты 54–55, 57–60.
- `docs/decisions/ADR-0005-vector-search-strategy.md` (retrieval gate).
- `docs/decisions/ADR-0007-llm-provider-integration.md`, раздел 46 (provenance), раздел 52 (evaluation).
- `docs/product/FUNCTIONAL_REQUIREMENTS.md`, FR-006, FR-011, FR-012, FR-015, FR-017, FR-060, и новые FR этой задачи (FR-061–FR-065).
- `docs/architecture/FORECAST_CONTRACT.md` (симметричный документ — выходной контракт, Phase 2.0).
- `docs/decisions/ADR-0012-realtime-and-background-monitoring-runtime.md` (Черновик, Phase 2.0).

---

## 5. История изменений статуса

| Дата | Статус | Кем изменён | Примечание |
|---|---|---|---|
| 2026-08-11 | На ревью | Claude (по решению Product Owner, Phase 0) | Создан по итогам PO-5/PO-6 (checkpoint-ратификация) — фиксирует инвентарь категорий контекста без разрешения на реализацию. |
| 2026-08-12 | На ревью | Claude (по решению Product Owner, Phase 2.0) | Добавлены §2.16 (indices), §2.17 (rates/yields), §2.18 (cross-asset claims); §2.1/§2.2 дополнены глобальным направлением акций (PO-2.0-1); §2.3 дополнен утверждёнными диапазонами горизонта (PO-2.0-3); §2.6 переведён из UNRESOLVED в MVP FOUNDATION (направление одобрено, источник остаётся открытым, PO-2.0-2). Ни одна категория не разрешена к реализации. |
