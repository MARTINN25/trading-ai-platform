"""AI quality evaluation harness for `GenerateInstrumentAnalysis` (ADR-0007 §52).

A first, deliberately small baseline — not a general-purpose benchmark
framework, not a replacement for human review, and not a claim that AI
quality is "solved." See `dataset.py` and this package's README section
for what it actually checks and what it deliberately does not.
"""

from __future__ import annotations
